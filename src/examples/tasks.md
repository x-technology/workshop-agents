# Practice tasks

1. Add a new output validator that rejects malformed JSON from the model.
2. Implement a tool call to store tasks in a simple JSON file.
3. Add concurrency to the orchestrator and keep per-email state isolated.
4. Extend the webhook to support a `dry_run` flag.
5. Add a metrics summary (counts per category + average latency).
